apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cralyx-wildcard-cert
  namespace: default
spec:
  secretName: cralyx-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.cralyx.com"
    - cralyx.com
